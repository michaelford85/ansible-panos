<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="css/main.css">
</head>
<body>


<table border="1">
    <thead>
        <tr>
            <th>Firewall</th>
            <th>Current ACLs</th>
        </tr>
    </thead>
    {% for host in groups['ec2'] %}
    <tbody>
        <tr>
            <td align='center'>{{ hostvars[host]['tags']['hostname'] }}</td>
            <!-- <td align='center'><a href="http://{{ webserver_fqdn}}/{{ hostvars[host]['tags']['hostname'] }}.html">Rules Link</a></td> -->
            <td align='center'><a href="https://s3.amazonaws.com/{{firewall_inventory_webpage_name|lower}}.{{dns_zone}}/{{hostvars[host]['tags']['hostname']|lower}}.html">Rules Link</a></td>
            <!-- "s3.amazonaws.com/{{firewall_inventory_webpage_name|lower}}.{{dns_zone}}/{{hostvars[inventory_hostname]['tags']['hostname']|lower}}.html" -->
        </tr>
    </tbody>
    {% endfor %}
</table>



</body>
</html>
